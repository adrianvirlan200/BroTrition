import { Button } from "@nextui-org/react";

const AddExerciseButton = () => {
  return (
    <Button variant="light" color="primary" className="text-lg font-medium">
      ADD EXERCISE
    </Button>
  );
};

export default AddExerciseButton;
