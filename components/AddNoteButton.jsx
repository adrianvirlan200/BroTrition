import { Button } from "@nextui-org/react";

const AddNoteButton = () => {
  return (
    <Button variant="light" color="warning" className="text-lg font-medium">
      ADD NOTE
    </Button>
  );
};

export default AddNoteButton;
